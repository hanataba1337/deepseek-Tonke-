# DeepSeek Token Monitor 🖥️📊

> 💸 **你的每一分钱都花在哪了？让它在桌面上直接告诉你。**

实时监控 DeepSeek API Token 用量的桌面悬浮窗工具。  
**直接读取 CC-Switch 内部数据库**，零侵入，无需改任何配置。

## 快速使用

**直接运行（无需安装 Python）：**

```
双击 DeepSeekMonitor.exe
```

**或者通过 Python 源码运行：**

```bash
pip install -r requirements.txt
python main.py
```

> **前置条件**：需要已安装 [CC-Switch](https://cc-switch.com) 并配置了 DeepSeek 提供商。

## 效果预览

```
┌──────────────────────────────────┐
│  DeepSeek 用量监控           ×   │
│  ● 读取 CC-Switch               │
│  ────────────────────────────    │
│  ── 今日 ──                     │
│    输入:              1,234      │
│    输出:                567      │
│    总计:              1,801      │
│    费用:            ¥0.0012      │
│    预算: ¥[50.00]     剩¥37.52   │
│  ── 模型 ──                     │
│    V4-Flash   1,000  ¥0.0008    │
│    V4-Pro       200  ¥0.0004    │
│  ────────────────────────────   │
│    请求: 108    状态: HTTP 200   │
└──────────────────────────────────┘
```

## 功能特性

- **零侵入** — 直接从 CC-Switch 数据库读取数据，无需任何 API Key 或配置
- **实时悬浮窗** — 桌面常驻半透明窗口，每 2 秒自动刷新，可拖拽
- **费用统计** — 今日输入/输出 Token 数及预估费用
- **月度预算** — 自行设定每月预算上限，实时显示已用/剩余，低于 20% 变色提醒
- **按模型拆分** — V4-Flash / V4-Pro 用量和费用分别显示，方便对比
- **绿色免安装** — 提供单文件 exe，双击即用，无需 Python 环境

## 悬浮窗交互

| 操作 | 说明 |
|------|------|
| **拖拽标题栏** | 移动悬浮窗到任意位置 |
| **点击 ×** | 关闭程序 |
| **编辑预算框** | 直接输入数字，回车保存 |  |

### 显示内容说明

| 区域 | 说明 |
|------|------|
| ● 状态灯 | 绿色=运行中，红色=异常 |
| 今日 | 当天累计 Token 输入/输出/总计及费用 |
| 预算 | 月度预算、已用金额、剩余金额 |
| 模型 | V4-Flash / V4-Pro 各自的 Token 用量及费用 |

## 工作原理

```
你发消息 → CC-Switch(15721端口) → DeepSeek 官方
                ↓
      CC-Switch 自动记录每次请求的 token 数到 SQLite 数据库
                ↓
      DeepSeek Monitor 读取该数据库并在悬浮窗显示
```

不需要代理，不需要修改任何配置，CC-Switch 本身就已经记录了所有用量数据。

## 项目结构

```
deepseek-monitor/
├── main.py              # 主入口
├── tracker.py           # 读取 CC-Switch 数据库获取用量
├── overlay.py           # 桌面悬浮窗 UI（tkinter 透明窗口）
├── settings.py          # 用户设置持久化（budget 等）
├── config.py            # 配置（价格、窗口样式等）
├── DeepSeekMonitor.exe  # 绿色免安装可执行文件
├── requirements.txt     # Python 依赖（源码运行需要）
├── start.bat            # 一键启动脚本
└── .gitignore           # Git 忽略规则
```

## 费用计算

默认价格（基于 DeepSeek 官方定价，¥/百万 Token）：

| 模型 | 输入(缓存未命中) | 输入(缓存命中) | 输出 |
|------|:---:|:---:|:---:|
| DeepSeek-V4-Flash | ¥1.00 | ¥0.02 | ¥2.00 |
| DeepSeek-V4-Pro | ¥3.00 | ¥0.025 | ¥6.00 |

> 注：CC-Switch 数据库未记录缓存命中状态，费用按缓存未命中价计算（保守估计）。
> 
> 可在 `config.py` 的 `PRICING` 字典中自定义。

## 数据来源

数据来自 **CC-Switch** 的 `proxy_request_logs` 表，位于：

```
%USERPROFILE%\.cc-switch\cc-switch.db
```

## 常见问题

**Q: 需要安装 CC-Switch 吗？**
A: 是的，本工具依赖 CC-Switch 记录的用量数据。

**Q: 会影响网络性能吗？**
A: 完全不会。本工具只读数据库，不代理任何网络请求。

**Q: 预算怎么改？**
A: 直接在悬浮窗的预算输入框里输入数字，按回车保存。也可以删除 `settings.json` 恢复默认。

## 许可证

MIT
